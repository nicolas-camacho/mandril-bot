name: Deploy
on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    permissions:
      id-token: write # Needed for auth with Deno Deploy
      contents: read # Needed to clone the repository

    steps:
      - name: Clone repository
        uses: actions/checkout@v3

      - name: Install Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: "bun install" # 📝 Update the build command(s) if necessary

      - name: Rename variable for bun to build
        run: "sed -i 's/var getZlibSync = .*;/var getZlibSync = null;/' example.js"

      - name: Build step
        run: "bun build --target=bun ./index.ts --outfile=index.js"
        
      - name: Upload to Deno Deploy
        uses: denoland/deployctl@v1
        with:
          project: "sour-cow-94"
          entrypoint: "index.js" # 📝 Update the entrypoint if necessary
          root: "." # 📝 Update the root if necessary
